# 公開情報（2026-09-26 時点の現在の状態）

セットアップは完了し、**すでに誰でも使える状態**です。

## URL

| 用途 | URL |
|---|---|
| 生徒画面（掲示・QR用） | https://yatoukousuke-hue.github.io/juku-help/ |
| 講師画面 | https://yatoukousuke-hue.github.io/juku-help/teacher.html |
| 掲示用 QR ページ | https://yatoukousuke-hue.github.io/juku-help/qr.html |
| ソースコード（GitHub） | https://github.com/yatoukousuke-hue/juku-help |
| データベース（Supabase） | https://supabase.com/dashboard/project/lwrjktjccutdhticabsi |

- 講師画面の合言葉の初期値：`sensei` → **最初に講師画面の「設定」から変更してください**
- 公開先は Netlify ではなく **GitHub Pages** にしました（GitHub にログイン済みだったため、追加のアカウントなしで公開できたため）。ブロック対策の考え方は同じです（`github.io` がブロックされる場合は Netlify / Cloudflare Pages に `app` フォルダを置き直せば移行できます）。

## Supabase について

- 組織：yatoukousuke-hue's Org（Pro プラン）に `juku-help` プロジェクトを作成。リージョン東京。
- 稼働コンピュート MICRO のため **月 $10 の追加課金**が発生します（作成時に同意済み）。一時停止はしません。
- データベースのパスワードは Supabase が自動生成したものです。通常は使いませんが、必要になったら Project Settings → Database → Reset database password で再設定できます。
- 表の中身は Supabase の **Table Editor** で見られます（`requests` = 依頼、`students` = 名簿、`app_settings` = 合言葉）。
- テスト送信した依頼が 1 件（「動作テスト（消してOK）」、状態 = 取消）残っています。消したい場合は Table Editor の `requests` から削除してください。

## 更新のしかた

画面のファイルは GitHub の `main` ブランチに push すると **1〜2 分で自動公開**されます。

### 方法A：GitHub のサイト上で編集（パソコンのブラウザだけで完結）
1. https://github.com/yatoukousuke-hue/juku-help/blob/main/app/config.js を開く
2. 右上の鉛筆アイコン（Edit）を押す
3. 書き換えて、右上の **Commit changes...** → **Commit changes**
4. 1〜2 分後に公開 URL に反映（反映状況は https://github.com/yatoukousuke-hue/juku-help/actions で確認）

### 方法B：このフォルダで編集して push
```bash
git add -A
git commit -m "設定変更"
git push
```

### 方法C：Claude に頼む
「教室名を〇〇に変えて公開して」のように伝えれば、編集から push まで行えます。

## 初回にやること（残作業）

1. 講師画面 → 設定 → **名簿を貼り付けて取り込む**（スプレッドシートの 生徒番号・氏名・学年・学校名 の 4 列をコピー）
2. 講師画面 → 設定 → **合言葉を変更**
3. `qr.html` を印刷して掲示
4. 講師に「講師画面 URL」「合言葉」「ホーム画面に追加の方法」を共有
5. 学校支給タブレット 1 台で生徒画面が開けるか確認
